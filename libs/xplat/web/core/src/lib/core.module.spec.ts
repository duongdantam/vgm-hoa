describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { FyCoreModule } from './core.module';

describe('FyCoreModule', () => {
  it('should work', () => {
    expect(new FyCoreModule(null)).toBeDefined();
  });
});
*/
